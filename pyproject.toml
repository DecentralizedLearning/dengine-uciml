[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dengine-uciml"
version = "0.1.0"
description = "dEngine extension for UCI Machine Learning Repository datasets"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dengine",
    "scikit-learn",
]

[project.optional-dependencies]
dev = [
    "pytest"
]

[tool.setuptools.packages.find]
include = ["dengine_uciml*"]

[tool.setuptools.data-files]
"share/dengine-uciml/configs" = [
  "configs/*.*",
  "configs/**/*.*"
]
